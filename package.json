{
  "name": "tdd-stack-in-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "ui-dev": "vite",
    "ui-build": "vite build",
    "ui-preview": "vite preview",
    "backend-test-watch": "jest --watchAll --verbose --coverage",
    "backend-test": "jest --verbose --coverage",
    "ui-test": "cypress open"
  },
  "devDependencies": {
    "@types/jest": "^29.2.6",
    "cypress": "^12.3.0",
    "vite": "^4.0.0"
  },
  "dependencies": {
    "jest": "^29.3.1"
  },
  "jest": {
    "moduleFileExtensions": ["js", "jsx"],
    "moduleDirectories": ["node_modules", "src"]
  }
}
